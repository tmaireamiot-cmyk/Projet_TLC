# Projet_TLC

# 1 Modifier le code avant de lancer :
    -> Ligne 11 du __Test__.java changer le chemin absolu

# 2 Lancer le fichier __Test__.java
    -> Avant de compiler : se placer dans le répertorie /output
    -> Pour compiler : javac -cp "antlr-3.5.2-complete.jar" *.java
    -> Pour exécuter le code :
            Sur Linux ou Mac -> java -cp ".:antlr-3.5.2-complete.jar" __Test__
            Sur Windows -> java -cp ".;antlr-3.5.2-complete.jar" __Test__